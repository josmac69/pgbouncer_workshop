; PgBouncer user list
; Format: "username" "password_hash"
; For md5: md5 + md5(password + username)
; Example: for user "admin" with password "admin123":
; echo -n "admin123admin" | md5sum -> then prepend "md5"

; Admin user - full access to admin console
; Admin user - full access to admin console
"admin" "admin123"

; Monitor user - read-only stats access
"monitor" "monitor"

; Test users for connection limit demonstration
; user1 - will be limited by max_user_connections (3)
"user1" "user1"

; user2 - will be limited by max_user_connections (3)
"user2" "user2"

; user3 - another user for testing
"user3" "user3"
